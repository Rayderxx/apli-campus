<section class="profile" ng-controller="ProfileCtrl">
    <div class="header">
        <span class="back"><a href="/trombi"><i class="icon-arrow-left"></i></a></span>
        <img src="{{student.picture}}" alt="" class="thumb-picture img-responsive"><span class="name">{{student.name}}</span>
        <br>{{student.formation}}
    </div>
</section>